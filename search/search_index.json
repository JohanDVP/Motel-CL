{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Documentaci\u00f3n del Proyecto Motel","text":"<p>Proyecto: Motelandro</p> <p>Sistema de gesti\u00f3n integral para el motel Motelandro, dise\u00f1ado para optimizar el control de habitaciones, el registro de clientes y la administraci\u00f3n de reservas en tiempo real.</p> <p>Que hace el proyecto?</p> <p>El proyecto cuenta con 3 funcionalidades especificas para 3 operaciones principales, las cuales son mirar, agregar y eliminar, estas 3 funcionalidades estan implementadas para el registro de usuarios, la creacion de habitaciones y la reserva de estas mismas.</p> <p>Instalaci\u00f3n y Ejecuci\u00f3n:</p> <p>1) Necesitas por obligaci\u00f3n tener uv instalado, dirigete al siguiente link y sigue los pasos de instalacion que se te muestra en la pagina dependiendo de tu sistema operativo:</p> <p>https://docs.astral.sh/uv/getting-started/installation/</p> <p>Luego de esto, clona el repositorio e instala todas las dependencias, ejecutando los siguientes comandos en la terminal:</p> <p>git clone (aqui va el link del repositorio) cd motelandro uv sync (Esto lee el archivo donde estan listadas todas las dependencias y las instala automaticamente como typer, pytest, etc)</p> <p>2) Ejecutar Backend (FastAPI):</p> <p>uv run uvicorn src.api.main:app --reload</p> <p>3) Ejecutar Frontend (Streamlit):</p> <p>uv run streamlit run src/app/main.py</p> <p>Calidad y Automatizaci\u00f3n:</p> <p>1) Pruebas: (Para ejecutar las pruebas y verificar la cobertura)</p> <p>uv run pytest --cov=src</p> <p>2) Linting y Calidad: (Para verificar el estilo de c\u00f3digo con Ruff y la complejidad con Radon)</p> <p>uv run ruff check src/ uv run radon cc src/ -a -s</p> <p>Stack tecnologico Utilizado:</p> <ol> <li> <p>Backend (API y L\u00f3gica)</p> <p>FastAPI: Framework moderno de alto rendimiento para construir APIs REST.</p> <p>Pydantic: Utilizado para la validaci\u00f3n de datos y la creaci\u00f3n de esquemas (contratos de datos).</p> <p>Python 3.12: Lenguaje base del proyecto.</p> </li> <li> <p>Base de Datos y Persistencia</p> <p>Supabase: Plataforma Backend-as-a-Service basada en PostgreSQL, utilizada para almacenar y gestionar los datos del sistema.</p> <p>Supabase-py: Librer\u00eda cliente para la integraci\u00f3n directa con Python.</p> </li> <li> <p>Frontend (Interfaz de Usuario)</p> <p>Streamlit: Framework para crear aplicaciones web orientadas a datos de forma r\u00e1pida, conectando el frontend directamente con la API mediante peticiones requests.</p> </li> <li> <p>Calidad, Automatizaci\u00f3n y DevOps</p> <p>uv: Gestor de paquetes ultrarr\u00e1pido y gestor de entornos virtuales.</p> <p>Pytest: Framework para pruebas unitarias e integraci\u00f3n.</p> <p>Ruff: Linter y formateador de c\u00f3digo extremadamente r\u00e1pido para garantizar el cumplimiento de est\u00e1ndares.</p> <p>Radon: Herramienta para medir la complejidad ciclom\u00e1tica del c\u00f3digo (garantizando mantenibilidad \"Grado A\").</p> <p>MkDocs (con Material theme): Generador de documentaci\u00f3n t\u00e9cnica est\u00e1tica.</p> <p>GitHub Actions: Automatizaci\u00f3n de workflows para CI/CD (integraci\u00f3n y despliegue continuo).</p> </li> </ol>"},{"location":"api/","title":"Documentaci\u00f3n de la API","text":""},{"location":"api/#src.api.routers.reserva","title":"<code>src.api.routers.reserva</code>","text":"<p>Router de FastAPI para los endpoints de Reservas.</p>"},{"location":"api/#src.api.routers.reserva.actualizar_reserva","title":"<code>actualizar_reserva(reserva_id, command, service=Depends(get_reserva_service))</code>","text":"<p>Actualiza los detalles de programaci\u00f3n o habitaciones de una reserva existente.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El ID de la reserva a actualizar.</p> required <code>command</code> <code>ReservaCreate</code> <p>Nuevos datos de la reserva.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la reserva no existe (404) o los datos son inv\u00e1lidos (400).</p> <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <p>El objeto de la reserva actualizada.</p> Source code in <code>src/api/routers/reserva.py</code> <pre><code>@router.put(\"/{reserva_id}\", response_model=ReservaResponse)\ndef actualizar_reserva(\n    reserva_id: int,\n    command: ReservaCreate,\n    service: ReservaService = Depends(get_reserva_service),\n):\n    \"\"\"Actualiza los detalles de programaci\u00f3n o habitaciones de una reserva existente.\n\n    Args:\n        reserva_id (int): El ID de la reserva a actualizar.\n        command (ReservaCreate): Nuevos datos de la reserva.\n\n    Raises:\n        HTTPException: Si la reserva no existe (404) o los datos son inv\u00e1lidos (400).\n\n    Returns:\n        ReservaResponse: El objeto de la reserva actualizada.\n    \"\"\"\n    try:\n        return service.actualizar(reserva_id, command)\n    except ReservaNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.reserva.buscar_reserva","title":"<code>buscar_reserva(reserva_id, service=Depends(get_reserva_service))</code>","text":"<p>Busca los detalles de una reserva espec\u00edfica por su ID.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El identificador \u00fanico de la reserva.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la reserva no existe (404).</p> <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <p>Los detalles de la reserva solicitada.</p> Source code in <code>src/api/routers/reserva.py</code> <pre><code>@router.get(\"/{reserva_id}\", response_model=ReservaResponse)\ndef buscar_reserva(\n    reserva_id: int, service: ReservaService = Depends(get_reserva_service)\n):\n    \"\"\"Busca los detalles de una reserva espec\u00edfica por su ID.\n\n    Args:\n        reserva_id (int): El identificador \u00fanico de la reserva.\n\n    Raises:\n        HTTPException: Si la reserva no existe (404).\n\n    Returns:\n        ReservaResponse: Los detalles de la reserva solicitada.\n    \"\"\"\n    try:\n        return service.buscar(reserva_id)\n    except ReservaNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n</code></pre>"},{"location":"api/#src.api.routers.reserva.cancelar_reserva","title":"<code>cancelar_reserva(reserva_id, service=Depends(get_reserva_service))</code>","text":"<p>Cancela de forma segura una reserva activa y libera la habitaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El ID de la reserva a cancelar.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la reserva no existe (404) o la operaci\u00f3n falla (400).</p> <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <p>El objeto de la reserva actualizada.</p> Source code in <code>src/api/routers/reserva.py</code> <pre><code>@router.post(\"/{reserva_id}/cancelar\", response_model=ReservaResponse)\ndef cancelar_reserva(\n    reserva_id: int, service: ReservaService = Depends(get_reserva_service)\n):\n    \"\"\"Cancela de forma segura una reserva activa y libera la habitaci\u00f3n.\n\n    Args:\n        reserva_id (int): El ID de la reserva a cancelar.\n\n    Raises:\n        HTTPException: Si la reserva no existe (404) o la operaci\u00f3n falla (400).\n\n    Returns:\n        ReservaResponse: El objeto de la reserva actualizada.\n    \"\"\"\n    try:\n        return service.cancelar(reserva_id)\n    except ReservaNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.reserva.crear_reserva","title":"<code>crear_reserva(command, service=Depends(get_reserva_service))</code>","text":"<p>Crea un nuevo registro de reserva.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>ReservaCreate</code> <p>Datos necesarios para crear la reserva.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la habitaci\u00f3n no est\u00e1 disponible o los datos son inv\u00e1lidos (400).</p> <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <p>El objeto de la reserva reci\u00e9n creada.</p> Source code in <code>src/api/routers/reserva.py</code> <pre><code>@router.post(\"/\", response_model=ReservaResponse, status_code=status.HTTP_201_CREATED)\ndef crear_reserva(\n    command: ReservaCreate, service: ReservaService = Depends(get_reserva_service)\n):\n    \"\"\"Crea un nuevo registro de reserva.\n\n    Args:\n        command (ReservaCreate): Datos necesarios para crear la reserva.\n\n    Raises:\n        HTTPException: Si la habitaci\u00f3n no est\u00e1 disponible o los\n            datos son inv\u00e1lidos (400).\n\n    Returns:\n        ReservaResponse: El objeto de la reserva reci\u00e9n creada.\n    \"\"\"\n    try:\n        return service.crear(command)\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.reserva.eliminar_reserva","title":"<code>eliminar_reserva(reserva_id, service=Depends(get_reserva_service))</code>","text":"<p>Elimina permanentemente un registro de reserva de la base de datos.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El ID de la reserva a eliminar.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la reserva no existe (404) o no se puede eliminar (400).</p> Source code in <code>src/api/routers/reserva.py</code> <pre><code>@router.delete(\"/{reserva_id}\", status_code=status.HTTP_204_NO_CONTENT)\ndef eliminar_reserva(\n    reserva_id: int, service: ReservaService = Depends(get_reserva_service)\n):\n    \"\"\"Elimina permanentemente un registro de reserva de la base de datos.\n\n    Args:\n        reserva_id (int): El ID de la reserva a eliminar.\n\n    Raises:\n        HTTPException: Si la reserva no existe (404) o no se puede eliminar (400).\n    \"\"\"\n    try:\n        service.eliminar(reserva_id)\n    except ReservaNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.reserva.listar_reservas","title":"<code>listar_reservas(service=Depends(get_reserva_service))</code>","text":"<p>Obtiene todas las reservas registradas en el sistema.</p> <p>Returns:</p> Type Description <p>list[ReservaResponse]: Una lista con todas las reservas activas y pasadas.</p> Source code in <code>src/api/routers/reserva.py</code> <pre><code>@router.get(\"/\", response_model=list[ReservaResponse])\ndef listar_reservas(service: ReservaService = Depends(get_reserva_service)):\n    \"\"\"Obtiene todas las reservas registradas en el sistema.\n\n    Returns:\n        list[ReservaResponse]: Una lista con todas las reservas activas y pasadas.\n    \"\"\"\n    return service.listar()\n</code></pre>"},{"location":"api/#src.api.routers.room","title":"<code>src.api.routers.room</code>","text":"<p>Router de FastAPI para los endpoints de Habitaciones.</p>"},{"location":"api/#src.api.routers.room.actualizar_habitacion","title":"<code>actualizar_habitacion(id_room, room, service=Depends(get_room_service))</code>","text":"<p>Actualiza caracter\u00edsticas t\u00e9cnicas o precios de una habitaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>El ID de la habitaci\u00f3n a actualizar.</p> required <code>room</code> <code>RoomCreate</code> <p>Nuevos datos de la habitaci\u00f3n.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la habitaci\u00f3n no se encuentra (404) o es inv\u00e1lida (400).</p> <p>Returns:</p> Name Type Description <code>RoomResponse</code> <p>El objeto de la habitaci\u00f3n actualizado.</p> Source code in <code>src/api/routers/room.py</code> <pre><code>@router.put(\"/{id_room}\", response_model=RoomResponse)\ndef actualizar_habitacion(\n    id_room: int,\n    room: RoomCreate,\n    service: RoomService = Depends(get_room_service),\n):\n    \"\"\"Actualiza caracter\u00edsticas t\u00e9cnicas o precios de una habitaci\u00f3n.\n\n    Args:\n        id_room (int): El ID de la habitaci\u00f3n a actualizar.\n        room (RoomCreate): Nuevos datos de la habitaci\u00f3n.\n\n    Raises:\n        HTTPException: Si la habitaci\u00f3n no se encuentra (404) o es inv\u00e1lida (400).\n\n    Returns:\n        RoomResponse: El objeto de la habitaci\u00f3n actualizado.\n    \"\"\"\n    try:\n        return service.actualizar(id_room, room)\n    except RoomNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.room.buscar_habitacion","title":"<code>buscar_habitacion(id_room, service=Depends(get_room_service))</code>","text":"<p>Obtiene informaci\u00f3n detallada de una habitaci\u00f3n espec\u00edfica.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>El ID de la habitaci\u00f3n.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si la habitaci\u00f3n no se encuentra (404).</p> <p>Returns:</p> Name Type Description <code>RoomResponse</code> <p>Informaci\u00f3n de la habitaci\u00f3n.</p> Source code in <code>src/api/routers/room.py</code> <pre><code>@router.get(\"/{id_room}\", response_model=RoomResponse)\ndef buscar_habitacion(id_room: int, service: RoomService = Depends(get_room_service)):\n    \"\"\"Obtiene informaci\u00f3n detallada de una habitaci\u00f3n espec\u00edfica.\n\n    Args:\n        id_room (int): El ID de la habitaci\u00f3n.\n\n    Raises:\n        HTTPException: Si la habitaci\u00f3n no se encuentra (404).\n\n    Returns:\n        RoomResponse: Informaci\u00f3n de la habitaci\u00f3n.\n    \"\"\"\n    try:\n        return service.buscar(id_room)\n    except RoomNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n</code></pre>"},{"location":"api/#src.api.routers.room.crear_habitacion","title":"<code>crear_habitacion(room, service=Depends(get_room_service))</code>","text":"<p>Registra una nueva habitaci\u00f3n en el sistema.</p> <p>Parameters:</p> Name Type Description Default <code>room</code> <code>RoomCreate</code> <p>Datos para la nueva habitaci\u00f3n.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si los datos son inv\u00e1lidos (400).</p> <p>Returns:</p> Name Type Description <code>RoomResponse</code> <p>La habitaci\u00f3n reci\u00e9n creada.</p> Source code in <code>src/api/routers/room.py</code> <pre><code>@router.post(\"/\", response_model=RoomResponse, status_code=status.HTTP_201_CREATED)\ndef crear_habitacion(\n    room: RoomCreate, service: RoomService = Depends(get_room_service)\n):\n    \"\"\"Registra una nueva habitaci\u00f3n en el sistema.\n\n    Args:\n        room (RoomCreate): Datos para la nueva habitaci\u00f3n.\n\n    Raises:\n        HTTPException: Si los datos son inv\u00e1lidos (400).\n\n    Returns:\n        RoomResponse: La habitaci\u00f3n reci\u00e9n creada.\n    \"\"\"\n    try:\n        return service.crear(room)\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.room.eliminar_habitacion","title":"<code>eliminar_habitacion(id_room, service=Depends(get_room_service))</code>","text":"<p>Elimina el registro de una habitaci\u00f3n del sistema.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>El ID de la habitaci\u00f3n a eliminar.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si no se encuentra (404) o no se puede eliminar (400).</p> Source code in <code>src/api/routers/room.py</code> <pre><code>@router.delete(\"/{id_room}\", status_code=status.HTTP_204_NO_CONTENT)\ndef eliminar_habitacion(id_room: int, service: RoomService = Depends(get_room_service)):\n    \"\"\"Elimina el registro de una habitaci\u00f3n del sistema.\n\n    Args:\n        id_room (int): El ID de la habitaci\u00f3n a eliminar.\n\n    Raises:\n        HTTPException: Si no se encuentra (404) o no se puede eliminar (400).\n    \"\"\"\n    try:\n        service.eliminar(id_room)\n    except RoomNoEncontradaError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.room.listar_habitaciones","title":"<code>listar_habitaciones(disponibles_only=False, service=Depends(get_room_service))</code>","text":"<p>Obtiene las habitaciones, filtrando opcionalmente por disponibilidad.</p> <p>Parameters:</p> Name Type Description Default <code>disponibles_only</code> <code>bool</code> <p>Si es True, solo lista habitaciones libres.</p> <code>False</code> <p>Returns:</p> Type Description <p>list[RoomResponse]: Lista de habitaciones seg\u00fan el filtro.</p> Source code in <code>src/api/routers/room.py</code> <pre><code>@router.get(\"/\", response_model=list[RoomResponse])\ndef listar_habitaciones(\n    disponibles_only: bool = False,\n    service: RoomService = Depends(get_room_service),\n):\n    \"\"\"Obtiene las habitaciones, filtrando opcionalmente por disponibilidad.\n\n    Args:\n        disponibles_only (bool): Si es True, solo lista habitaciones libres.\n\n    Returns:\n        list[RoomResponse]: Lista de habitaciones seg\u00fan el filtro.\n    \"\"\"\n    if disponibles_only:\n        return service.listar_disponibles()\n    return service.listar()\n</code></pre>"},{"location":"api/#src.api.routers.user","title":"<code>src.api.routers.user</code>","text":"<p>Router de FastAPI para los endpoints de Usuarios.</p>"},{"location":"api/#src.api.routers.user.actualizar_usuario","title":"<code>actualizar_usuario(id_user, usuario, service=Depends(get_user_service))</code>","text":"<p>Actualiza los detalles del perfil de un usuario existente.</p> <p>Parameters:</p> Name Type Description Default <code>id_user</code> <code>int</code> <p>El ID del usuario a actualizar.</p> required <code>usuario</code> <code>UsuarioCreate</code> <p>Nuevos datos del perfil de usuario.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si el usuario no existe (404) o los datos son inv\u00e1lidos (400).</p> <p>Returns:</p> Name Type Description <code>UsuarioResponse</code> <p>El objeto de usuario actualizado.</p> Source code in <code>src/api/routers/user.py</code> <pre><code>@router.put(\"/{id_user}\", response_model=UsuarioResponse)\ndef actualizar_usuario(\n    id_user: int,\n    usuario: UsuarioCreate,\n    service: UsuarioService = Depends(get_user_service),\n):\n    \"\"\"Actualiza los detalles del perfil de un usuario existente.\n\n    Args:\n        id_user (int): El ID del usuario a actualizar.\n        usuario (UsuarioCreate): Nuevos datos del perfil de usuario.\n\n    Raises:\n        HTTPException: Si el usuario no existe (404) o los datos son inv\u00e1lidos (400).\n\n    Returns:\n        UsuarioResponse: El objeto de usuario actualizado.\n    \"\"\"\n    try:\n        return service.actualizar(id_user, usuario)\n    except UsuarioNoEncontradoError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.user.buscar_usuario","title":"<code>buscar_usuario(id_user, service=Depends(get_user_service))</code>","text":"<p>Busca un usuario espec\u00edfico por su ID.</p> <p>Parameters:</p> Name Type Description Default <code>id_user</code> <code>int</code> <p>El ID \u00fanico del usuario.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si el usuario no es encontrado (404).</p> <p>Returns:</p> Name Type Description <code>UsuarioResponse</code> <p>El usuario solicitado.</p> Source code in <code>src/api/routers/user.py</code> <pre><code>@router.get(\"/{id_user}\", response_model=UsuarioResponse)\ndef buscar_usuario(id_user: int, service: UsuarioService = Depends(get_user_service)):\n    \"\"\"Busca un usuario espec\u00edfico por su ID.\n\n    Args:\n        id_user (int): El ID \u00fanico del usuario.\n\n    Raises:\n        HTTPException: Si el usuario no es encontrado (404).\n\n    Returns:\n        UsuarioResponse: El usuario solicitado.\n    \"\"\"\n    try:\n        return service.buscar(id_user)\n    except UsuarioNoEncontradoError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n</code></pre>"},{"location":"api/#src.api.routers.user.eliminar_usuario","title":"<code>eliminar_usuario(id_user, service=Depends(get_user_service))</code>","text":"<p>Elimina un usuario de la base de datos.</p> <p>Parameters:</p> Name Type Description Default <code>id_user</code> <code>int</code> <p>El ID del usuario a eliminar.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si el usuario no existe (404) o no se puede eliminar (400).</p> Source code in <code>src/api/routers/user.py</code> <pre><code>@router.delete(\"/{id_user}\", status_code=status.HTTP_204_NO_CONTENT)\ndef eliminar_usuario(id_user: int, service: UsuarioService = Depends(get_user_service)):\n    \"\"\"Elimina un usuario de la base de datos.\n\n    Args:\n        id_user (int): El ID del usuario a eliminar.\n\n    Raises:\n        HTTPException: Si el usuario no existe (404) o no se puede eliminar (400).\n    \"\"\"\n    try:\n        service.eliminar(id_user)\n    except UsuarioNoEncontradoError as e:\n        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(e)) from e\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"api/#src.api.routers.user.listar_usuarios","title":"<code>listar_usuarios(service=Depends(get_user_service))</code>","text":"<p>Obtiene la lista completa de usuarios registrados.</p> <p>Returns:</p> Type Description <p>list[UsuarioResponse]: Una lista con los perfiles de usuario.</p> Source code in <code>src/api/routers/user.py</code> <pre><code>@router.get(\"/\", response_model=list[UsuarioResponse])\ndef listar_usuarios(service: UsuarioService = Depends(get_user_service)):\n    \"\"\"Obtiene la lista completa de usuarios registrados.\n\n    Returns:\n        list[UsuarioResponse]: Una lista con los perfiles de usuario.\n    \"\"\"\n    return service.listar()\n</code></pre>"},{"location":"api/#src.api.routers.user.registrar_usuario","title":"<code>registrar_usuario(usuario, service=Depends(get_user_service))</code>","text":"<p>Registra un nuevo usuario en el sistema.</p> <p>Parameters:</p> Name Type Description Default <code>usuario</code> <code>UsuarioCreate</code> <p>Datos del nuevo usuario a registrar.</p> required <p>Raises:</p> Type Description <code>HTTPException</code> <p>Si los datos son inv\u00e1lidos (400).</p> <p>Returns:</p> Name Type Description <code>UsuarioResponse</code> <p>El usuario reci\u00e9n registrado.</p> Source code in <code>src/api/routers/user.py</code> <pre><code>@router.post(\"/\", response_model=UsuarioResponse, status_code=status.HTTP_201_CREATED)\ndef registrar_usuario(\n    usuario: UsuarioCreate, service: UsuarioService = Depends(get_user_service)\n):\n    \"\"\"Registra un nuevo usuario en el sistema.\n\n    Args:\n        usuario (UsuarioCreate): Datos del nuevo usuario a registrar.\n\n    Raises:\n        HTTPException: Si los datos son inv\u00e1lidos (400).\n\n    Returns:\n        UsuarioResponse: El usuario reci\u00e9n registrado.\n    \"\"\"\n    try:\n        return service.registrar(usuario)\n    except MotelError as e:\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST, detail=str(e)\n        ) from e\n</code></pre>"},{"location":"modelo_er/","title":"Modelo Entidad-Relaci\u00f3n","text":"<p>A continuaci\u00f3n se presenta el esquema relacional de la base de datos, mostrando la relaci\u00f3n entre usuarios, habitaciones y reservas.</p> <p></p>"},{"location":"service/","title":"L\u00f3gica de Negocio","text":""},{"location":"service/#src.services.reservation_service","title":"<code>src.services.reservation_service</code>","text":"<p>Servicio de l\u00f3gica de negocio que maneja el ciclo de vida de las  reservas del motel.</p>"},{"location":"service/#src.services.reservation_service.ReservaService","title":"<code>ReservaService</code>","text":"<p>Clase de servicio para gestionar las operaciones de negocio de reservas del motel.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>class ReservaService:\n    \"\"\"Clase de servicio para gestionar las operaciones de negocio de reservas del\n    motel.\"\"\"\n\n    def __init__(\n        self,\n        repository: ReservationRepository,\n        room_service: RoomService,\n        user_service: UsuarioService,\n    ) -&gt; None:\n        \"\"\"Inicializa el servicio de reservas con las dependencias requeridas.\"\"\"\n        self._repository = repository\n        self._room_service = room_service\n        self._user_service = user_service\n\n    def listar(self) -&gt; list[ReservaResponse]:\n        \"\"\"Recupera el historial completo de reservas desde el repositorio.\n\n        Returns:\n            list[ReservaResponse]: Una lista de todas las reservas registradas.\n        \"\"\"\n        return self._repository.get_all()\n\n    def buscar(self, reserva_id: int) -&gt; ReservaResponse:\n        \"\"\"Busca una reserva por su ID.\n\n        Args:\n            reserva_id (int): El identificador \u00fanico de la reserva.\n\n        Returns:\n            ReservaResponse: El objeto de la reserva encontrada.\n\n        Raises:\n            ReservaNoEncontradaError: Si no existe una reserva con el ID\n                proporcionado.\n        \"\"\"\n        reserva = self._repository.get_by_id(reserva_id)\n        if reserva is None:\n            raise ReservaNoEncontradaError(f\"La reserva {reserva_id} no existe.\")\n        return reserva\n\n    def crear(self, command: ReservaCreate) -&gt; ReservaResponse:\n        \"\"\"Crea una nueva reserva, actualiza la ocupaci\u00f3n de la habitaci\u00f3n y\n        persiste los datos.\n\n        Args:\n            command (ReservaCreate): Datos que contienen el ID de usuario,\n                ID de habitaci\u00f3n y horas.\n\n        Returns:\n            ReservaResponse: La reserva reci\u00e9n creada.\n\n        Raises:\n            RoomNoDisponibleError: Si la habitaci\u00f3n solicitada ya est\u00e1 ocupada.\n        \"\"\"\n        usuario = self._user_service.buscar(command.id_usuario)\n        room = self._room_service.buscar(command.id_room)\n\n        if room.reservada_por is not None:\n            raise RoomNoDisponibleError(\n                f\"La habitaci\u00f3n {room.id} ya est\u00e1 reservada.\"\n            )\n\n        total_calculado = room.precio * command.horas\n\n        payload = {\n            \"id_usuario\": usuario.id_user,\n            \"id_room\": room.id,\n            \"horas\": command.horas,\n            \"total\": total_calculado,\n            \"estado\": \"activa\",\n        }\n\n        reserva_guardada = self._repository.create(payload)\n        self._room_service.marcar_como_ocupada(room.id, usuario.id_user)\n        return reserva_guardada\n\n    def cancelar(self, reserva_id: int) -&gt; ReservaResponse:\n        \"\"\"Cancela una reserva activa y libera completamente la habitaci\u00f3n.\n\n        Args:\n            reserva_id (int): El ID de la reserva a cancelar.\n\n        Returns:\n            ReservaResponse: El objeto de la reserva actualizada.\n\n        Raises:\n            DatosInvalidosError: Si la reserva no est\u00e1 en estado 'activa'.\n        \"\"\"\n        reserva = self.buscar(reserva_id)\n\n        if reserva.estado != \"activa\":\n            raise DatosInvalidosError(\n                \"Solo se pueden cancelar reservas en estado 'activa'.\"\n            )\n\n        reserva_actualizada = self._repository.update_status(reserva_id, \"cancelada\")\n        self._room_service.liberar_habitacion(reserva.id_room)\n        return reserva_actualizada\n\n    def actualizar(self, reserva_id: int, command: ReservaCreate) -&gt; ReservaResponse:\n        \"\"\"Actualiza los detalles de la reserva y recalcula el precio final.\n\n        Args:\n            reserva_id (int): El ID de la reserva a actualizar.\n            command (ReservaCreate): Nuevos datos para la reserva.\n\n        Returns:\n            ReservaResponse: El objeto de la reserva actualizada.\n        \"\"\"\n        self.buscar(reserva_id)\n\n        self._user_service.buscar(command.id_usuario)\n        room = self._room_service.buscar(command.id_room)\n\n        total_calculado = room.precio * command.horas\n\n        payload = {\n            \"id_usuario\": command.id_usuario,\n            \"id_room\": command.id_room,\n            \"horas\": command.horas,\n            \"total\": total_calculado,\n        }\n        return self._repository.update(reserva_id, payload)\n\n    def eliminar(self, reserva_id: int) -&gt; None:\n        \"\"\"Elimina un registro de reserva y libera de forma segura su habitaci\u00f3n\n        asociada.\n\n        Args:\n            reserva_id (int): El ID de la reserva a eliminar.\n        \"\"\"\n        reserva = self.buscar(reserva_id)\n        if reserva.estado == \"activa\":\n            self._room_service.liberar_habitacion(reserva.id_room)\n        self._repository.delete(reserva_id)\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.__init__","title":"<code>__init__(repository, room_service, user_service)</code>","text":"<p>Inicializa el servicio de reservas con las dependencias requeridas.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>def __init__(\n    self,\n    repository: ReservationRepository,\n    room_service: RoomService,\n    user_service: UsuarioService,\n) -&gt; None:\n    \"\"\"Inicializa el servicio de reservas con las dependencias requeridas.\"\"\"\n    self._repository = repository\n    self._room_service = room_service\n    self._user_service = user_service\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.actualizar","title":"<code>actualizar(reserva_id, command)</code>","text":"<p>Actualiza los detalles de la reserva y recalcula el precio final.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El ID de la reserva a actualizar.</p> required <code>command</code> <code>ReservaCreate</code> <p>Nuevos datos para la reserva.</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>El objeto de la reserva actualizada.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>def actualizar(self, reserva_id: int, command: ReservaCreate) -&gt; ReservaResponse:\n    \"\"\"Actualiza los detalles de la reserva y recalcula el precio final.\n\n    Args:\n        reserva_id (int): El ID de la reserva a actualizar.\n        command (ReservaCreate): Nuevos datos para la reserva.\n\n    Returns:\n        ReservaResponse: El objeto de la reserva actualizada.\n    \"\"\"\n    self.buscar(reserva_id)\n\n    self._user_service.buscar(command.id_usuario)\n    room = self._room_service.buscar(command.id_room)\n\n    total_calculado = room.precio * command.horas\n\n    payload = {\n        \"id_usuario\": command.id_usuario,\n        \"id_room\": command.id_room,\n        \"horas\": command.horas,\n        \"total\": total_calculado,\n    }\n    return self._repository.update(reserva_id, payload)\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.buscar","title":"<code>buscar(reserva_id)</code>","text":"<p>Busca una reserva por su ID.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El identificador \u00fanico de la reserva.</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>El objeto de la reserva encontrada.</p> <p>Raises:</p> Type Description <code>ReservaNoEncontradaError</code> <p>Si no existe una reserva con el ID proporcionado.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>def buscar(self, reserva_id: int) -&gt; ReservaResponse:\n    \"\"\"Busca una reserva por su ID.\n\n    Args:\n        reserva_id (int): El identificador \u00fanico de la reserva.\n\n    Returns:\n        ReservaResponse: El objeto de la reserva encontrada.\n\n    Raises:\n        ReservaNoEncontradaError: Si no existe una reserva con el ID\n            proporcionado.\n    \"\"\"\n    reserva = self._repository.get_by_id(reserva_id)\n    if reserva is None:\n        raise ReservaNoEncontradaError(f\"La reserva {reserva_id} no existe.\")\n    return reserva\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.cancelar","title":"<code>cancelar(reserva_id)</code>","text":"<p>Cancela una reserva activa y libera completamente la habitaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El ID de la reserva a cancelar.</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>El objeto de la reserva actualizada.</p> <p>Raises:</p> Type Description <code>DatosInvalidosError</code> <p>Si la reserva no est\u00e1 en estado 'activa'.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>def cancelar(self, reserva_id: int) -&gt; ReservaResponse:\n    \"\"\"Cancela una reserva activa y libera completamente la habitaci\u00f3n.\n\n    Args:\n        reserva_id (int): El ID de la reserva a cancelar.\n\n    Returns:\n        ReservaResponse: El objeto de la reserva actualizada.\n\n    Raises:\n        DatosInvalidosError: Si la reserva no est\u00e1 en estado 'activa'.\n    \"\"\"\n    reserva = self.buscar(reserva_id)\n\n    if reserva.estado != \"activa\":\n        raise DatosInvalidosError(\n            \"Solo se pueden cancelar reservas en estado 'activa'.\"\n        )\n\n    reserva_actualizada = self._repository.update_status(reserva_id, \"cancelada\")\n    self._room_service.liberar_habitacion(reserva.id_room)\n    return reserva_actualizada\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.crear","title":"<code>crear(command)</code>","text":"<p>Crea una nueva reserva, actualiza la ocupaci\u00f3n de la habitaci\u00f3n y persiste los datos.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>ReservaCreate</code> <p>Datos que contienen el ID de usuario, ID de habitaci\u00f3n y horas.</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>La reserva reci\u00e9n creada.</p> <p>Raises:</p> Type Description <code>RoomNoDisponibleError</code> <p>Si la habitaci\u00f3n solicitada ya est\u00e1 ocupada.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>def crear(self, command: ReservaCreate) -&gt; ReservaResponse:\n    \"\"\"Crea una nueva reserva, actualiza la ocupaci\u00f3n de la habitaci\u00f3n y\n    persiste los datos.\n\n    Args:\n        command (ReservaCreate): Datos que contienen el ID de usuario,\n            ID de habitaci\u00f3n y horas.\n\n    Returns:\n        ReservaResponse: La reserva reci\u00e9n creada.\n\n    Raises:\n        RoomNoDisponibleError: Si la habitaci\u00f3n solicitada ya est\u00e1 ocupada.\n    \"\"\"\n    usuario = self._user_service.buscar(command.id_usuario)\n    room = self._room_service.buscar(command.id_room)\n\n    if room.reservada_por is not None:\n        raise RoomNoDisponibleError(\n            f\"La habitaci\u00f3n {room.id} ya est\u00e1 reservada.\"\n        )\n\n    total_calculado = room.precio * command.horas\n\n    payload = {\n        \"id_usuario\": usuario.id_user,\n        \"id_room\": room.id,\n        \"horas\": command.horas,\n        \"total\": total_calculado,\n        \"estado\": \"activa\",\n    }\n\n    reserva_guardada = self._repository.create(payload)\n    self._room_service.marcar_como_ocupada(room.id, usuario.id_user)\n    return reserva_guardada\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.eliminar","title":"<code>eliminar(reserva_id)</code>","text":"<p>Elimina un registro de reserva y libera de forma segura su habitaci\u00f3n asociada.</p> <p>Parameters:</p> Name Type Description Default <code>reserva_id</code> <code>int</code> <p>El ID de la reserva a eliminar.</p> required Source code in <code>src/services/reservation_service.py</code> <pre><code>def eliminar(self, reserva_id: int) -&gt; None:\n    \"\"\"Elimina un registro de reserva y libera de forma segura su habitaci\u00f3n\n    asociada.\n\n    Args:\n        reserva_id (int): El ID de la reserva a eliminar.\n    \"\"\"\n    reserva = self.buscar(reserva_id)\n    if reserva.estado == \"activa\":\n        self._room_service.liberar_habitacion(reserva.id_room)\n    self._repository.delete(reserva_id)\n</code></pre>"},{"location":"service/#src.services.reservation_service.ReservaService.listar","title":"<code>listar()</code>","text":"<p>Recupera el historial completo de reservas desde el repositorio.</p> <p>Returns:</p> Type Description <code>list[ReservaResponse]</code> <p>list[ReservaResponse]: Una lista de todas las reservas registradas.</p> Source code in <code>src/services/reservation_service.py</code> <pre><code>def listar(self) -&gt; list[ReservaResponse]:\n    \"\"\"Recupera el historial completo de reservas desde el repositorio.\n\n    Returns:\n        list[ReservaResponse]: Una lista de todas las reservas registradas.\n    \"\"\"\n    return self._repository.get_all()\n</code></pre>"},{"location":"storage/","title":"Capa de Acceso a Datos","text":""},{"location":"storage/#src.storage.reservation_repository","title":"<code>src.storage.reservation_repository</code>","text":"<p>Implementaci\u00f3n del Repositorio de Reservas utilizando Supabase.</p>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository","title":"<code>ReservationRepository</code>","text":"<p>Clase de repositorio para gestionar registros de reservas en Supabase.</p> Source code in <code>src/storage/reservation_repository.py</code> <pre><code>class ReservationRepository:\n    \"\"\"Clase de repositorio para gestionar registros de reservas en Supabase.\"\"\"\n\n    def __init__(self, client: Client) -&gt; None:\n        \"\"\"Inicializa el repositorio con una instancia del cliente de Supabase.\n\n        Args:\n            client (Client): El cliente autenticado de Supabase.\n        \"\"\"\n        self.client = client\n        self.table = \"reservas\"\n\n    def create(self, reservation_data: dict) -&gt; ReservaResponse:\n        \"\"\"Inserta un nuevo registro de reserva en la base de datos.\n\n        Args:\n            reservation_data (dict): Diccionario que contiene los detalles\n                de la reserva.\n\n        Returns:\n            ReservaResponse: El objeto de reserva validado que se ha creado.\n        \"\"\"\n        response = self.client.table(self.table).insert(reservation_data).execute()\n        return ReservaResponse.model_validate(response.data[0])\n\n    def get_all(self) -&gt; list[ReservaResponse]:\n        \"\"\"Recupera el historial completo de reservas desde la base de datos.\n\n        Returns:\n            list[ReservaResponse]: Una lista de todos los objetos de reserva\n                existentes.\n        \"\"\"\n        response = self.client.table(self.table).select(\"*\").execute()\n        return [ReservaResponse.model_validate(res) for res in response.data]\n\n    def get_by_id(self, id_reserva: int) -&gt; ReservaResponse | None:\n        \"\"\"Busca una reserva espec\u00edfica por su identificador \u00fanico.\n\n        Args:\n            id_reserva (int): El ID de la reserva a recuperar.\n\n        Returns:\n            ReservaResponse | None: El objeto de reserva si se encuentra,\n                de lo contrario None.\n        \"\"\"\n        response = (\n            self.client.table(self.table)\n            .select(\"*\")\n            .eq(\"id\", id_reserva)\n            .execute()\n        )\n        if not response.data:\n            return None\n        return ReservaResponse.model_validate(response.data[0])\n\n    def update_status(self, id_reserva: int, nuevo_estado: str) -&gt; ReservaResponse:\n        \"\"\"Actualiza el campo de estado de una reserva existente.\n\n        Args:\n            id_reserva (int): El ID de la reserva a actualizar.\n            nuevo_estado (str): El nuevo estado (ej. 'activa', 'cancelada').\n\n        Returns:\n            ReservaResponse: El objeto de reserva actualizado.\n        \"\"\"\n        response = (\n            self.client.table(self.table)\n            .update({\"estado\": nuevo_estado})\n            .eq(\"id\", id_reserva)\n            .execute()\n        )\n        return ReservaResponse.model_validate(response.data[0])\n\n    def update(self, id_reserva: int, reservation_data: dict) -&gt; ReservaResponse:\n        \"\"\"Actualiza los campos de datos estructurales de una reserva existente.\n\n        Args:\n            id_reserva (int): El ID de la reserva a actualizar.\n            reservation_data (dict): Diccionario con los campos a actualizar.\n\n        Returns:\n            ReservaResponse: El objeto de reserva actualizado.\n        \"\"\"\n        response = (\n            self.client.table(self.table)\n            .update(reservation_data)\n            .eq(\"id\", id_reserva)\n            .execute()\n        )\n        return ReservaResponse.model_validate(response.data[0])\n\n    def delete(self, id_reserva: int) -&gt; None:\n        \"\"\"Elimina permanentemente un registro de reserva de la base de datos.\n\n        Args:\n            id_reserva (int): El ID de la reserva a eliminar.\n        \"\"\"\n        self.client.table(self.table).delete().eq(\"id\", id_reserva).execute()\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.__init__","title":"<code>__init__(client)</code>","text":"<p>Inicializa el repositorio con una instancia del cliente de Supabase.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Client</code> <p>El cliente autenticado de Supabase.</p> required Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def __init__(self, client: Client) -&gt; None:\n    \"\"\"Inicializa el repositorio con una instancia del cliente de Supabase.\n\n    Args:\n        client (Client): El cliente autenticado de Supabase.\n    \"\"\"\n    self.client = client\n    self.table = \"reservas\"\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.create","title":"<code>create(reservation_data)</code>","text":"<p>Inserta un nuevo registro de reserva en la base de datos.</p> <p>Parameters:</p> Name Type Description Default <code>reservation_data</code> <code>dict</code> <p>Diccionario que contiene los detalles de la reserva.</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>El objeto de reserva validado que se ha creado.</p> Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def create(self, reservation_data: dict) -&gt; ReservaResponse:\n    \"\"\"Inserta un nuevo registro de reserva en la base de datos.\n\n    Args:\n        reservation_data (dict): Diccionario que contiene los detalles\n            de la reserva.\n\n    Returns:\n        ReservaResponse: El objeto de reserva validado que se ha creado.\n    \"\"\"\n    response = self.client.table(self.table).insert(reservation_data).execute()\n    return ReservaResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.delete","title":"<code>delete(id_reserva)</code>","text":"<p>Elimina permanentemente un registro de reserva de la base de datos.</p> <p>Parameters:</p> Name Type Description Default <code>id_reserva</code> <code>int</code> <p>El ID de la reserva a eliminar.</p> required Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def delete(self, id_reserva: int) -&gt; None:\n    \"\"\"Elimina permanentemente un registro de reserva de la base de datos.\n\n    Args:\n        id_reserva (int): El ID de la reserva a eliminar.\n    \"\"\"\n    self.client.table(self.table).delete().eq(\"id\", id_reserva).execute()\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.get_all","title":"<code>get_all()</code>","text":"<p>Recupera el historial completo de reservas desde la base de datos.</p> <p>Returns:</p> Type Description <code>list[ReservaResponse]</code> <p>list[ReservaResponse]: Una lista de todos los objetos de reserva existentes.</p> Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def get_all(self) -&gt; list[ReservaResponse]:\n    \"\"\"Recupera el historial completo de reservas desde la base de datos.\n\n    Returns:\n        list[ReservaResponse]: Una lista de todos los objetos de reserva\n            existentes.\n    \"\"\"\n    response = self.client.table(self.table).select(\"*\").execute()\n    return [ReservaResponse.model_validate(res) for res in response.data]\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.get_by_id","title":"<code>get_by_id(id_reserva)</code>","text":"<p>Busca una reserva espec\u00edfica por su identificador \u00fanico.</p> <p>Parameters:</p> Name Type Description Default <code>id_reserva</code> <code>int</code> <p>El ID de la reserva a recuperar.</p> required <p>Returns:</p> Type Description <code>ReservaResponse | None</code> <p>ReservaResponse | None: El objeto de reserva si se encuentra, de lo contrario None.</p> Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def get_by_id(self, id_reserva: int) -&gt; ReservaResponse | None:\n    \"\"\"Busca una reserva espec\u00edfica por su identificador \u00fanico.\n\n    Args:\n        id_reserva (int): El ID de la reserva a recuperar.\n\n    Returns:\n        ReservaResponse | None: El objeto de reserva si se encuentra,\n            de lo contrario None.\n    \"\"\"\n    response = (\n        self.client.table(self.table)\n        .select(\"*\")\n        .eq(\"id\", id_reserva)\n        .execute()\n    )\n    if not response.data:\n        return None\n    return ReservaResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.update","title":"<code>update(id_reserva, reservation_data)</code>","text":"<p>Actualiza los campos de datos estructurales de una reserva existente.</p> <p>Parameters:</p> Name Type Description Default <code>id_reserva</code> <code>int</code> <p>El ID de la reserva a actualizar.</p> required <code>reservation_data</code> <code>dict</code> <p>Diccionario con los campos a actualizar.</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>El objeto de reserva actualizado.</p> Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def update(self, id_reserva: int, reservation_data: dict) -&gt; ReservaResponse:\n    \"\"\"Actualiza los campos de datos estructurales de una reserva existente.\n\n    Args:\n        id_reserva (int): El ID de la reserva a actualizar.\n        reservation_data (dict): Diccionario con los campos a actualizar.\n\n    Returns:\n        ReservaResponse: El objeto de reserva actualizado.\n    \"\"\"\n    response = (\n        self.client.table(self.table)\n        .update(reservation_data)\n        .eq(\"id\", id_reserva)\n        .execute()\n    )\n    return ReservaResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.reservation_repository.ReservationRepository.update_status","title":"<code>update_status(id_reserva, nuevo_estado)</code>","text":"<p>Actualiza el campo de estado de una reserva existente.</p> <p>Parameters:</p> Name Type Description Default <code>id_reserva</code> <code>int</code> <p>El ID de la reserva a actualizar.</p> required <code>nuevo_estado</code> <code>str</code> <p>El nuevo estado (ej. 'activa', 'cancelada').</p> required <p>Returns:</p> Name Type Description <code>ReservaResponse</code> <code>ReservaResponse</code> <p>El objeto de reserva actualizado.</p> Source code in <code>src/storage/reservation_repository.py</code> <pre><code>def update_status(self, id_reserva: int, nuevo_estado: str) -&gt; ReservaResponse:\n    \"\"\"Actualiza el campo de estado de una reserva existente.\n\n    Args:\n        id_reserva (int): El ID de la reserva a actualizar.\n        nuevo_estado (str): El nuevo estado (ej. 'activa', 'cancelada').\n\n    Returns:\n        ReservaResponse: El objeto de reserva actualizado.\n    \"\"\"\n    response = (\n        self.client.table(self.table)\n        .update({\"estado\": nuevo_estado})\n        .eq(\"id\", id_reserva)\n        .execute()\n    )\n    return ReservaResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#repositorio-de-habitaciones","title":"Repositorio de Habitaciones","text":""},{"location":"storage/#src.storage.room_repository","title":"<code>src.storage.room_repository</code>","text":"<p>Implementaci\u00f3n del Repositorio de Habitaciones utilizando Supabase.</p>"},{"location":"storage/#src.storage.room_repository.RoomRepository","title":"<code>RoomRepository</code>","text":"<p>Clase de repositorio para gestionar las habitaciones en Supabase.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>class RoomRepository:\n    \"\"\"Clase de repositorio para gestionar las habitaciones en Supabase.\"\"\"\n\n    def __init__(self, client: Client) -&gt; None:\n        \"\"\"Inicializa el repositorio con una instancia del cliente de Supabase.\n\n        Args:\n            client (Client): El cliente autenticado de Supabase.\n        \"\"\"\n        self.client = client\n        self.table = \"rooms\"\n\n    def get_all(self) -&gt; list[RoomResponse]:\n        \"\"\"Obtiene todas las habitaciones del motel.\n\n        Returns:\n            list[RoomResponse]: Lista de todos los objetos de habitaci\u00f3n.\n        \"\"\"\n        response = self.client.table(self.table).select(\"*\").execute()\n        return [RoomResponse.model_validate(room) for room in response.data]\n\n    def get_by_id(self, id_room: int) -&gt; RoomResponse | None:\n        \"\"\"Busca una habitaci\u00f3n espec\u00edfica por su ID.\n\n        Args:\n            id_room (int): El identificador \u00fanico de la habitaci\u00f3n.\n\n        Returns:\n            RoomResponse | None: La habitaci\u00f3n si existe, de lo contrario None.\n        \"\"\"\n        response = self.client.table(self.table).select(\"*\").eq(\"id\", id_room).execute()\n        if not response.data:\n            return None\n        return RoomResponse.model_validate(response.data[0])\n\n    def update_reservation(self, id_room: int, id_usuario: int | None) -&gt; RoomResponse:\n        \"\"\"Actualiza el estado de ocupaci\u00f3n de una habitaci\u00f3n.\n\n        Args:\n            id_room (int): El ID de la habitaci\u00f3n.\n            id_usuario (int | None): ID del usuario que reserva o None para liberar.\n\n        Returns:\n            RoomResponse: El objeto de la habitaci\u00f3n actualizada.\n        \"\"\"\n        response = (\n            self.client.table(self.table)\n            .update({\"reservada_por\": id_usuario})\n            .eq(\"id\", id_room)\n            .execute()\n        )\n        return RoomResponse.model_validate(response.data[0])\n\n    def get_available(self) -&gt; list[RoomResponse]:\n        \"\"\"Obtiene \u00fanicamente las habitaciones que no est\u00e1n reservadas.\n\n        Returns:\n            list[RoomResponse]: Lista de habitaciones disponibles.\n        \"\"\"\n        response = (\n            self.client.table(self.table)\n            .select(\"*\")\n            .is_(\"reservada_por\", \"null\")\n            .execute()\n        )\n        return [RoomResponse.model_validate(room) for room in response.data]\n\n    def create(self, room_data: RoomCreate) -&gt; RoomResponse:\n        \"\"\"Inserta una nueva habitaci\u00f3n en la base de datos.\n\n        Args:\n            room_data (RoomCreate): Datos de la nueva habitaci\u00f3n.\n\n        Returns:\n            RoomResponse: La habitaci\u00f3n creada.\n        \"\"\"\n        payload = room_data.model_dump()\n        response = self.client.table(self.table).insert(payload).execute()\n        return RoomResponse.model_validate(response.data[0])\n\n    def update(self, id_room: int, room_data: RoomCreate) -&gt; RoomResponse:\n        \"\"\"Actualiza los detalles t\u00e9cnicos o precio de una habitaci\u00f3n existente.\n\n        Args:\n            id_room (int): ID de la habitaci\u00f3n a actualizar.\n            room_data (RoomCreate): Nuevos datos de la habitaci\u00f3n.\n\n        Returns:\n            RoomResponse: El objeto de la habitaci\u00f3n actualizado.\n        \"\"\"\n        payload = room_data.model_dump()\n        response = (\n            self.client.table(self.table)\n            .update(payload)\n            .eq(\"id\", id_room)\n            .execute()\n        )\n        return RoomResponse.model_validate(response.data[0])\n\n    def delete(self, id_room: int) -&gt; None:\n        \"\"\"Elimina permanentemente una habitaci\u00f3n del sistema.\n\n        Args:\n            id_room (int): ID de la habitaci\u00f3n a eliminar.\n        \"\"\"\n        self.client.table(self.table).delete().eq(\"id\", id_room).execute()\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.__init__","title":"<code>__init__(client)</code>","text":"<p>Inicializa el repositorio con una instancia del cliente de Supabase.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Client</code> <p>El cliente autenticado de Supabase.</p> required Source code in <code>src/storage/room_repository.py</code> <pre><code>def __init__(self, client: Client) -&gt; None:\n    \"\"\"Inicializa el repositorio con una instancia del cliente de Supabase.\n\n    Args:\n        client (Client): El cliente autenticado de Supabase.\n    \"\"\"\n    self.client = client\n    self.table = \"rooms\"\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.create","title":"<code>create(room_data)</code>","text":"<p>Inserta una nueva habitaci\u00f3n en la base de datos.</p> <p>Parameters:</p> Name Type Description Default <code>room_data</code> <code>RoomCreate</code> <p>Datos de la nueva habitaci\u00f3n.</p> required <p>Returns:</p> Name Type Description <code>RoomResponse</code> <code>RoomResponse</code> <p>La habitaci\u00f3n creada.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>def create(self, room_data: RoomCreate) -&gt; RoomResponse:\n    \"\"\"Inserta una nueva habitaci\u00f3n en la base de datos.\n\n    Args:\n        room_data (RoomCreate): Datos de la nueva habitaci\u00f3n.\n\n    Returns:\n        RoomResponse: La habitaci\u00f3n creada.\n    \"\"\"\n    payload = room_data.model_dump()\n    response = self.client.table(self.table).insert(payload).execute()\n    return RoomResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.delete","title":"<code>delete(id_room)</code>","text":"<p>Elimina permanentemente una habitaci\u00f3n del sistema.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>ID de la habitaci\u00f3n a eliminar.</p> required Source code in <code>src/storage/room_repository.py</code> <pre><code>def delete(self, id_room: int) -&gt; None:\n    \"\"\"Elimina permanentemente una habitaci\u00f3n del sistema.\n\n    Args:\n        id_room (int): ID de la habitaci\u00f3n a eliminar.\n    \"\"\"\n    self.client.table(self.table).delete().eq(\"id\", id_room).execute()\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.get_all","title":"<code>get_all()</code>","text":"<p>Obtiene todas las habitaciones del motel.</p> <p>Returns:</p> Type Description <code>list[RoomResponse]</code> <p>list[RoomResponse]: Lista de todos los objetos de habitaci\u00f3n.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>def get_all(self) -&gt; list[RoomResponse]:\n    \"\"\"Obtiene todas las habitaciones del motel.\n\n    Returns:\n        list[RoomResponse]: Lista de todos los objetos de habitaci\u00f3n.\n    \"\"\"\n    response = self.client.table(self.table).select(\"*\").execute()\n    return [RoomResponse.model_validate(room) for room in response.data]\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.get_available","title":"<code>get_available()</code>","text":"<p>Obtiene \u00fanicamente las habitaciones que no est\u00e1n reservadas.</p> <p>Returns:</p> Type Description <code>list[RoomResponse]</code> <p>list[RoomResponse]: Lista de habitaciones disponibles.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>def get_available(self) -&gt; list[RoomResponse]:\n    \"\"\"Obtiene \u00fanicamente las habitaciones que no est\u00e1n reservadas.\n\n    Returns:\n        list[RoomResponse]: Lista de habitaciones disponibles.\n    \"\"\"\n    response = (\n        self.client.table(self.table)\n        .select(\"*\")\n        .is_(\"reservada_por\", \"null\")\n        .execute()\n    )\n    return [RoomResponse.model_validate(room) for room in response.data]\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.get_by_id","title":"<code>get_by_id(id_room)</code>","text":"<p>Busca una habitaci\u00f3n espec\u00edfica por su ID.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>El identificador \u00fanico de la habitaci\u00f3n.</p> required <p>Returns:</p> Type Description <code>RoomResponse | None</code> <p>RoomResponse | None: La habitaci\u00f3n si existe, de lo contrario None.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>def get_by_id(self, id_room: int) -&gt; RoomResponse | None:\n    \"\"\"Busca una habitaci\u00f3n espec\u00edfica por su ID.\n\n    Args:\n        id_room (int): El identificador \u00fanico de la habitaci\u00f3n.\n\n    Returns:\n        RoomResponse | None: La habitaci\u00f3n si existe, de lo contrario None.\n    \"\"\"\n    response = self.client.table(self.table).select(\"*\").eq(\"id\", id_room).execute()\n    if not response.data:\n        return None\n    return RoomResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.update","title":"<code>update(id_room, room_data)</code>","text":"<p>Actualiza los detalles t\u00e9cnicos o precio de una habitaci\u00f3n existente.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>ID de la habitaci\u00f3n a actualizar.</p> required <code>room_data</code> <code>RoomCreate</code> <p>Nuevos datos de la habitaci\u00f3n.</p> required <p>Returns:</p> Name Type Description <code>RoomResponse</code> <code>RoomResponse</code> <p>El objeto de la habitaci\u00f3n actualizado.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>def update(self, id_room: int, room_data: RoomCreate) -&gt; RoomResponse:\n    \"\"\"Actualiza los detalles t\u00e9cnicos o precio de una habitaci\u00f3n existente.\n\n    Args:\n        id_room (int): ID de la habitaci\u00f3n a actualizar.\n        room_data (RoomCreate): Nuevos datos de la habitaci\u00f3n.\n\n    Returns:\n        RoomResponse: El objeto de la habitaci\u00f3n actualizado.\n    \"\"\"\n    payload = room_data.model_dump()\n    response = (\n        self.client.table(self.table)\n        .update(payload)\n        .eq(\"id\", id_room)\n        .execute()\n    )\n    return RoomResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.room_repository.RoomRepository.update_reservation","title":"<code>update_reservation(id_room, id_usuario)</code>","text":"<p>Actualiza el estado de ocupaci\u00f3n de una habitaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>id_room</code> <code>int</code> <p>El ID de la habitaci\u00f3n.</p> required <code>id_usuario</code> <code>int | None</code> <p>ID del usuario que reserva o None para liberar.</p> required <p>Returns:</p> Name Type Description <code>RoomResponse</code> <code>RoomResponse</code> <p>El objeto de la habitaci\u00f3n actualizada.</p> Source code in <code>src/storage/room_repository.py</code> <pre><code>def update_reservation(self, id_room: int, id_usuario: int | None) -&gt; RoomResponse:\n    \"\"\"Actualiza el estado de ocupaci\u00f3n de una habitaci\u00f3n.\n\n    Args:\n        id_room (int): El ID de la habitaci\u00f3n.\n        id_usuario (int | None): ID del usuario que reserva o None para liberar.\n\n    Returns:\n        RoomResponse: El objeto de la habitaci\u00f3n actualizada.\n    \"\"\"\n    response = (\n        self.client.table(self.table)\n        .update({\"reservada_por\": id_usuario})\n        .eq(\"id\", id_room)\n        .execute()\n    )\n    return RoomResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#repositorio-de-usuarios","title":"Repositorio de Usuarios","text":""},{"location":"storage/#src.storage.user_repository","title":"<code>src.storage.user_repository</code>","text":"<p>Implementaci\u00f3n del Repositorio de Usuarios utilizando Supabase.</p>"},{"location":"storage/#src.storage.user_repository.UserRepository","title":"<code>UserRepository</code>","text":"<p>Clase de repositorio para gestionar los usuarios en Supabase.</p> Source code in <code>src/storage/user_repository.py</code> <pre><code>class UserRepository:\n    \"\"\"Clase de repositorio para gestionar los usuarios en Supabase.\"\"\"\n\n    def __init__(self, client: Client) -&gt; None:\n        \"\"\"Inicializa el repositorio con una instancia del cliente de Supabase.\n\n        Args:\n            client (Client): El cliente autenticado de Supabase.\n        \"\"\"\n        self.client = client\n        self.table = \"usuarios\"\n\n    def create(self, user_data: UsuarioCreate) -&gt; UsuarioResponse:\n        \"\"\"Inserta un nuevo usuario en la base de datos.\n\n        Args:\n            user_data (UsuarioCreate): Objeto con los datos del usuario.\n\n        Returns:\n            UsuarioResponse: El usuario reci\u00e9n creado y validado.\n        \"\"\"\n        payload = user_data.model_dump()\n        response = self.client.table(self.table).insert(payload).execute()\n        return UsuarioResponse.model_validate(response.data[0])\n\n    def get_all(self) -&gt; list[UsuarioResponse]:\n        \"\"\"Obtiene todos los usuarios registrados.\n\n        Returns:\n            list[UsuarioResponse]: Lista con todos los objetos de usuario.\n        \"\"\"\n        response = self.client.table(self.table).select(\"*\").execute()\n        return [UsuarioResponse.model_validate(user) for user in response.data]\n\n    def get_by_id(self, id_user: int) -&gt; UsuarioResponse | None:\n        \"\"\"Busca un usuario por su ID \u00fanico.\n\n        Args:\n            id_user (int): El ID \u00fanico del usuario.\n\n        Returns:\n            UsuarioResponse | None: El usuario encontrado o None si no existe.\n        \"\"\"\n        response = (\n            self.client.table(self.table)\n            .select(\"*\")\n            .eq(\"id_user\", id_user)\n            .execute()\n        )\n        if not response.data:\n            return None\n        return UsuarioResponse.model_validate(response.data[0])\n\n    def update(self, id_user: int, user_data: UsuarioCreate) -&gt; UsuarioResponse:\n        \"\"\"Actualiza los datos de un usuario existente en Supabase.\n\n        Args:\n            id_user (int): ID del usuario a actualizar.\n            user_data (UsuarioCreate): Nuevos datos del usuario.\n\n        Returns:\n            UsuarioResponse: El objeto de usuario actualizado.\n        \"\"\"\n        payload = user_data.model_dump()\n        response = (\n            self.client.table(self.table)\n            .update(payload)\n            .eq(\"id_user\", id_user)\n            .execute()\n        )\n        return UsuarioResponse.model_validate(response.data[0])\n\n    def delete(self, id_user: int) -&gt; None:\n        \"\"\"Elimina un usuario de la base de datos por su ID.\n\n        Args:\n            id_user (int): El ID del usuario a eliminar.\n        \"\"\"\n        self.client.table(self.table).delete().eq(\"id_user\", id_user).execute()\n</code></pre>"},{"location":"storage/#src.storage.user_repository.UserRepository.__init__","title":"<code>__init__(client)</code>","text":"<p>Inicializa el repositorio con una instancia del cliente de Supabase.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Client</code> <p>El cliente autenticado de Supabase.</p> required Source code in <code>src/storage/user_repository.py</code> <pre><code>def __init__(self, client: Client) -&gt; None:\n    \"\"\"Inicializa el repositorio con una instancia del cliente de Supabase.\n\n    Args:\n        client (Client): El cliente autenticado de Supabase.\n    \"\"\"\n    self.client = client\n    self.table = \"usuarios\"\n</code></pre>"},{"location":"storage/#src.storage.user_repository.UserRepository.create","title":"<code>create(user_data)</code>","text":"<p>Inserta un nuevo usuario en la base de datos.</p> <p>Parameters:</p> Name Type Description Default <code>user_data</code> <code>UsuarioCreate</code> <p>Objeto con los datos del usuario.</p> required <p>Returns:</p> Name Type Description <code>UsuarioResponse</code> <code>UsuarioResponse</code> <p>El usuario reci\u00e9n creado y validado.</p> Source code in <code>src/storage/user_repository.py</code> <pre><code>def create(self, user_data: UsuarioCreate) -&gt; UsuarioResponse:\n    \"\"\"Inserta un nuevo usuario en la base de datos.\n\n    Args:\n        user_data (UsuarioCreate): Objeto con los datos del usuario.\n\n    Returns:\n        UsuarioResponse: El usuario reci\u00e9n creado y validado.\n    \"\"\"\n    payload = user_data.model_dump()\n    response = self.client.table(self.table).insert(payload).execute()\n    return UsuarioResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.user_repository.UserRepository.delete","title":"<code>delete(id_user)</code>","text":"<p>Elimina un usuario de la base de datos por su ID.</p> <p>Parameters:</p> Name Type Description Default <code>id_user</code> <code>int</code> <p>El ID del usuario a eliminar.</p> required Source code in <code>src/storage/user_repository.py</code> <pre><code>def delete(self, id_user: int) -&gt; None:\n    \"\"\"Elimina un usuario de la base de datos por su ID.\n\n    Args:\n        id_user (int): El ID del usuario a eliminar.\n    \"\"\"\n    self.client.table(self.table).delete().eq(\"id_user\", id_user).execute()\n</code></pre>"},{"location":"storage/#src.storage.user_repository.UserRepository.get_all","title":"<code>get_all()</code>","text":"<p>Obtiene todos los usuarios registrados.</p> <p>Returns:</p> Type Description <code>list[UsuarioResponse]</code> <p>list[UsuarioResponse]: Lista con todos los objetos de usuario.</p> Source code in <code>src/storage/user_repository.py</code> <pre><code>def get_all(self) -&gt; list[UsuarioResponse]:\n    \"\"\"Obtiene todos los usuarios registrados.\n\n    Returns:\n        list[UsuarioResponse]: Lista con todos los objetos de usuario.\n    \"\"\"\n    response = self.client.table(self.table).select(\"*\").execute()\n    return [UsuarioResponse.model_validate(user) for user in response.data]\n</code></pre>"},{"location":"storage/#src.storage.user_repository.UserRepository.get_by_id","title":"<code>get_by_id(id_user)</code>","text":"<p>Busca un usuario por su ID \u00fanico.</p> <p>Parameters:</p> Name Type Description Default <code>id_user</code> <code>int</code> <p>El ID \u00fanico del usuario.</p> required <p>Returns:</p> Type Description <code>UsuarioResponse | None</code> <p>UsuarioResponse | None: El usuario encontrado o None si no existe.</p> Source code in <code>src/storage/user_repository.py</code> <pre><code>def get_by_id(self, id_user: int) -&gt; UsuarioResponse | None:\n    \"\"\"Busca un usuario por su ID \u00fanico.\n\n    Args:\n        id_user (int): El ID \u00fanico del usuario.\n\n    Returns:\n        UsuarioResponse | None: El usuario encontrado o None si no existe.\n    \"\"\"\n    response = (\n        self.client.table(self.table)\n        .select(\"*\")\n        .eq(\"id_user\", id_user)\n        .execute()\n    )\n    if not response.data:\n        return None\n    return UsuarioResponse.model_validate(response.data[0])\n</code></pre>"},{"location":"storage/#src.storage.user_repository.UserRepository.update","title":"<code>update(id_user, user_data)</code>","text":"<p>Actualiza los datos de un usuario existente en Supabase.</p> <p>Parameters:</p> Name Type Description Default <code>id_user</code> <code>int</code> <p>ID del usuario a actualizar.</p> required <code>user_data</code> <code>UsuarioCreate</code> <p>Nuevos datos del usuario.</p> required <p>Returns:</p> Name Type Description <code>UsuarioResponse</code> <code>UsuarioResponse</code> <p>El objeto de usuario actualizado.</p> Source code in <code>src/storage/user_repository.py</code> <pre><code>def update(self, id_user: int, user_data: UsuarioCreate) -&gt; UsuarioResponse:\n    \"\"\"Actualiza los datos de un usuario existente en Supabase.\n\n    Args:\n        id_user (int): ID del usuario a actualizar.\n        user_data (UsuarioCreate): Nuevos datos del usuario.\n\n    Returns:\n        UsuarioResponse: El objeto de usuario actualizado.\n    \"\"\"\n    payload = user_data.model_dump()\n    response = (\n        self.client.table(self.table)\n        .update(payload)\n        .eq(\"id_user\", id_user)\n        .execute()\n    )\n    return UsuarioResponse.model_validate(response.data[0])\n</code></pre>"}]}